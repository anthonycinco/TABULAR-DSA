numpy>=1.21.0
matplotlib>=3.5.0
pandas>=1.3.0
scipy>=1.7.0
seaborn>=0.11.0
# Built-in Python modules (no installation needed):
# argparse, threading, time, json, pickle, collections, statistics
# NS3 is installed separately via system package manager
# Ubuntu/Debian: sudo apt-get install ns3-dev
# macOS: brew install ns3
# Windows: Download from https://www.nsnam.org/download/ 